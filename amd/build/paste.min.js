define(["jquery"],function($){return{init:function(courseid,pastetext){$(".section .divider.bulk-hidden").each(function(id,courseSection){var $coursesection=$(courseSection);var $button=$coursesection.find("[data-action=open-chooser]");var sectionId=$button.attr("data-sectionnum");var beforeModule=$button.attr("data-beforemod");if(!beforeModule){beforeModule=""}var urlreturn=encodeURIComponent(location.href.replace(M.cfg.wwwroot,""));var url=`${M.cfg.wwwroot}/local/copy/paste.php?courseid=${courseid}&section=${sectionId}`+`&beforemodule=${beforeModule}&returnurl=${urlreturn}`;$coursesection.find(".divider-content").addClass("d-flex").addClass("align-items-center").append(`<form action="${url}"
                                   method="post">
                                 <button class="btn add-content local_copy-paste-content p-1"
                                         type="submit">${pastetext}</button>
                             </form>`)})}}});